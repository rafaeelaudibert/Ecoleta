// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore because this is generated dynamically by react-native-dotenv
import { API_ENDPOINT, ASSETS_PATH, IMAGES_PATH } from 'react-native-dotenv'

export default {
  apiEndpoint: API_ENDPOINT as string,
  assetsPath: ASSETS_PATH as string,
  imagesPath: IMAGES_PATH as string
}
